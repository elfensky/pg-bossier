# pg-bossier

This is the **release branch** — intentionally empty until the first release.

Active development happens on the
[`develop`](https://github.com/elfensky/pg-bossier/tree/develop) branch, which
holds the full project: source, tests, `README.md`, and `CLAUDE.md`. Each
release is squash-merged here as a single commit.
